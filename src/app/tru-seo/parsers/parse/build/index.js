import build, { clearTreeCache, getTreeCacheSize } from './build'

/**
 * @module parse/build
 *
 * Module containing functions to build a tree for the analysis.
 */
export {
	build,
	clearTreeCache,
	getTreeCacheSize
}